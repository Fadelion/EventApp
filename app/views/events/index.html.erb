<div class="container">
  <div class="jumbotron bg-light p-5 rounded mb-4">
    <h1 class="display-4">Bienvenue sur EventBe!</h1>
    <p class="lead">Découvrez les événements à venir ou créez le vôtre.</p>
    <hr class="my-4">
    <p>Rejoignez notre communauté et participez à des événements incroyables.</p>
    <%= link_to "Créer un événement", new_event_path, class: "btn btn-primary btn-lg" %>
  </div>

  <h2 class="mb-4">Événements à venir</h2>

  <div class="row">
    <% if @events.any? %>
      <% @events.each do |event| %>
        <div class="col-md-4 mb-4">
          <div class="card h-100">
            <div class="card-body">
              <h5 class="card-title"><%= event.title %></h5>
              <h6 class="card-subtitle mb-2 text-muted"><%= event.start_date.strftime("%d/%m/%Y à %H:%M") %></h6>
              <p class="card-text"><%= event.description.truncate(100) %></p>
              <div class="d-flex justify-content-between align-items-center">
                <span class="badge bg-primary"><%= event.price > 0 ? "#{event.price}€" : "Gratuit" %></span>
                <%= link_to "Voir plus", event_path(event), class: "btn btn-sm btn-outline-primary" %>
              </div>
            </div>
            <div class="card-footer text-muted">
              <small><i class="bi bi-geo-alt"></i> <%= event.location %></small>
            </div>
          </div>
        </div>
      <% end %>
    <% else %>
      <div class="col-12">
        <div class="alert alert-info">
          Aucun événement n'est prévu pour le moment. Soyez le premier à en créer un !
        </div>
      </div>
    <% end %>
  </div>
</div>
